# 表格组件扩展：嵌套、多级表头、单选多选等

## 表格示例扩展：嵌套结构+slot内嵌两种方式实现（固定表头、多级表头）

参照官方示例，实现固定列和表头

参照官方示例，实现流体高度

参照官方示例，实现多级表头

改造VTable，使用JSON方式实现多级表头

- 开发VTableColumn组件，提取table-column的相关内容，简化VTable代码
- 在TableColumnType上添加children属性，用于传递嵌套表头
- 将VTable的table-column替换为VTableColumn
- 在table.vue中，新增测试用例，使用JSON方式传递多级表头配置
- 调整VTableColumn组件的TS类型设计，也需要`对外宽类型，对内窄类型`



