# 表格组件扩展：嵌套、多级表头、单选多选等

## 表格示例扩展：嵌套结构+slot内嵌两种方式实现（固定表头、多级表头）

参照官方示例，实现固定列和表头

参照官方示例，实现流体高度

参照官方示例，实现多级表头

改造VTable，使用JSON方式实现多级表头

- 开发VTableColumn组件，提取table-column的相关内容，简化VTable代码
- 在TableColumnType上添加children属性，用于传递嵌套表头
- 将VTable的table-column替换为VTableColumn
- 在table.vue中，新增测试用例，使用JSON方式传递多级表头配置
- 调整VTableColumn组件的TS类型设计，也需要`对外宽类型，对内窄类型`



## 表格事件传递：单选表格示例

参照官方示例，开始着手实现单选功能

实现ELTable相关事件的TS提示功能（$attrs透传功能虽然正常，但是没有TS提示）

- 在polyfill.d.ts中添加TableEmits接口，让AI按照EL官网定义所有的Table事件
- 在VTable的对外类型中添加TableEmits相关参数
- 在types.d.ts中定义对外类型VTableType，优化VTable的对外类型设计

 

## 表格扩展：单选多选表格（expose函数透传）

将示例表格拆分到两个页面，并使用el-tabs划分布局

在VTable中屏蔽pagination默认项，简化示例

在tsx中将事件的prevent修饰符替换为`e.preventDefault()`

在单选表格中测试行按钮上是否添加`e.stopPropagation()`的区别。点击按钮时阻止事件冒泡，避免行被选中

使用defineExpose进行方法透传，测试是否能调用ELTable组件方法

参照官方示例，实现多选功能



